# API proxy target (used by Vite dev server)
API_URL=http://localhost:3000

# Optional: Sentry DSN for error monitoring
VITE_SENTRY_DSN=

# Optional: Sentry auth token for source map uploads (production builds only)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=
