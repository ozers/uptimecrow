[build]
builder = "DOCKERFILE"
dockerfilePath = "apps/api/Dockerfile"
buildTarget = "production"

[deploy]
startCommand = "/bin/sh -c 'pnpm db:migrate && node dist/index.js'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
